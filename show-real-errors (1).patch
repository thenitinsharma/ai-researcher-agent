diff --git a/ai-researcher-frontend/components/ChatPanel.tsx b/ai-researcher-frontend/components/ChatPanel.tsx
index f6fe992..dd1b987 100644
--- a/ai-researcher-frontend/components/ChatPanel.tsx
+++ b/ai-researcher-frontend/components/ChatPanel.tsx
@@ -59,13 +59,13 @@ export default function ChatPanel() {
       setStatus("idle");
     } catch (err) {
       setStatus("error");
+      const detail = err instanceof Error ? err.message : String(err);
       setMessages((m) => [
         ...m,
         {
           id: crypto.randomUUID(),
           role: "assistant",
-          content:
-            "I couldn't reach the research backend just now. Check that the API server is running and NEXT_PUBLIC_API_URL is set correctly.",
+          content: `I couldn't reach the research backend just now.\n\n**Details:** ${detail}`,
           createdAt: Date.now(),
         },
       ]);
